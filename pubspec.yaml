name: food_recipe
description: A new Flutter project for food recipes.
version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.0
  sqflite: ^2.0.0
  path: ^1.8.0
  http: ^0.13.0
  flutter_staggered_grid_view: ^0.6.2
  cached_network_image: ^3.2.0
  font_awesome_flutter: ^10.4.0
  shared_preferences: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/
